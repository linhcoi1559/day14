//khai váo mảng 2 chiều
var arrInt = [];
//nhập dữ liệu cho mảng 2 chiều
let row = parseInt(prompt("nhập vào số dòng"))
let col = parseInt(prompt("nhập vào số cột"))
for (let i = 0; i < row; i++){
let arrRow = [];

}
for (let j = 0; j < col; j++){
    let elementij = parseInt(prompt("nhập giá trị phần tử"));
    arrRow.push(elementij);
}
arrInt.push(arrRow);

